// Predict and explain...
// What will happen when this program is run?
// Check your prediction and explanation by running the program. there are some syntax problems, after they are fixed it runs if
// grep 'assert' 200.js -C 3 
// Fix anything that needs fixing.

const person = {
    "name": "Jemima",
    "location": "",
    "id_number" :9,
};

console.assert(person.name === "Jemima");//true
console.assert(person.location === "Glasgow");//false
console.assert(person.id_number === 9);//true
